{
  "name": "kanban",
  "version": "0.0.0",
  "private": true,
  "description": " A kanban board to keep track of web dev work at the Kennedy Center .",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist --nojekyll"
  },
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@alpinejs/persist": "^3.15.12",
    "@alpinejs/sort": "^3.15.12",
    "alpinejs": "^3.15.11",
    "install": "^0.13.0",
    "npm": "^11.13.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^8.0.10"
  }
}
